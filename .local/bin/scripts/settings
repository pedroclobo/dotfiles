#!/usr/bin/sh

# Script that acts as my control panel.

choice=$(printf "Bluetooth\nBackup" | dmenu -i -p 'What to do? ')

case $choice in
	Bluetooth) sudo blueberry ;;
	Backup)  sudo timeshift-gtk ;;
	*) ;;
esac
