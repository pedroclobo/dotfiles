#!/bin/sh

brightness=$(light | sed "s/\...//g")

# Output
echo "  "$brightness"%"

# Buttons
case "$BUTTON" in
	2) "$TERMINAL" -e "$EDITOR" "$0" ;;
	4) brightness-change +5 ;;
	5) brightness-change -5 ;;
esac
