#!/bin/sh
# Status bar brightness script

# Buttons
case "$BUTTON" in
	2) "$TERMINAL" -e "$EDITOR" "$0" ;;
	4) brictl +5 ;;
	5) brictl -5 ;;
esac

# Get brightness level
brightness=$(light | sed "s/\...//g")

# Output
echo "💡 "$brightness"%"
